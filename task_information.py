from Participant import Participant
from Task import Task


def task_information(p:Participant, t:Task):
    file_name = f"DATA/participant_{p.id}/task_{p.id}_{t.id}.json"


    time_taken_in_sec = "Timestamp where participant realizes " - "Start (start of new slide) "# =T3-S3
